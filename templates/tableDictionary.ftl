package ${package};


public class ${tableName}{
	
	private Long ${column};
	
	public Long get${column?cap_first}() {
		return id;
	}
	public void set${column?cap_first}(Long ${column}) {
		this.${column} = ${column};
	}

	
	
}
